# Styles

Global design tokens and base styles live in `src/index.css` (Tailwind v4
CSS-first `@theme` block). This folder is reserved for any future
feature-specific stylesheets that don't fit as Tailwind utility classes
(e.g. third-party widget overrides, print stylesheets).
